# Copy to /srv/autoweb/secrets/host.env and chmod 600.
# Generate POSTGRES_PASSWORD with: openssl rand -hex 32
APP_RELEASE_SHA=c2c173636dad50364ef1a246f384c8c417296d8e
AUTOWEB_APP_ENV_FILE=/srv/autoweb/secrets/app.env
POSTGRES_DB=newsweb_explain
POSTGRES_USER=newsweb
POSTGRES_PASSWORD=replace-with-64-hex-characters
NEWSWEB_POLLING_ENABLED=false
ACME_EMAIL=replace@example.com
PREVIEW_ALLOWED_CIDRS=203.0.113.10/32
ALERT_EMAIL=replace@example.com
CADDYFILE_PATH=./Caddyfile.preview
